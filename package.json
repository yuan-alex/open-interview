{
  "name": "open-interview",
  "private": true,
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "format": "biome format . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
